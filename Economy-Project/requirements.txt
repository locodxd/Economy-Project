# =====================================================
# DEPENDENCIAS DEL BOT DE ECONOMÍA
# =====================================================

discord.py>=2.0.0
aiohttp>=3.8.0
requests>=2.28.0
python-dotenv>=0.19.0
# opcional para testear
pytest>=7.0.0
pytest-asyncio>=0.18.0

# Image Processing (para avatares y embeds)
Pillow>=9.0.0

# GIF System (para efectos visuales en embeds)
tenorpy>=1.0.0

# =====================================================
# NOTAS:
# - No se requiere sqlalchemy (usamos JSON local)
# - Para instalar: pip install -r requirements.txt
# =====================================================